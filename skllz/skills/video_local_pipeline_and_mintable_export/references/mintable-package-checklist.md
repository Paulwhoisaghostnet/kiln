# Mintable Package Checklist

- Output includes `index.html` and package manifest metadata.
- All media, images, and scripts are local to the package directory.
- No external CDN/runtime URLs are required for playback.
- Exported artifact is validated directly in browser from local folder.
- Compile/export run logs include package size and validation captures.
- Final package is ready to archive/zip without structural edits.
