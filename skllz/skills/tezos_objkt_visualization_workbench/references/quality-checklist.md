# Quality Checklist

- Query windows are bounded and explicitly documented.
- Primary/fallback source behavior is visible to users.
- Canonical normalization map is applied before rendering.
- Empty/error/stale/rate-limit UX states are implemented.
- Deterministic debug hook exposes core provenance and counters.
- Row-tier readability checks pass with decimation where required.
